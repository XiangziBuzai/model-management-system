# 模型管理系统 - 技术文档

## 1. 项目概述

### 1.1 项目简介

**模型管理系统**是一个基于 Vue 3 + Spring Boot 的全栈 Web 应用，用于管理模型和工具数据。系统支持多用户数据隔离，每个用户只能查看和管理自己的数据，并提供了模型广场功能，支持公开模型的浏览、收藏和交易。

### 1.2 核心价值

- **数据管理**: 提供完整的模型和工具 CRUD 操作
- **数据隔离**: 多租户架构，用户数据相互隔离
- **批量导入**: 支持 Excel 批量导入数据
- **数据统计**: 实时统计图表展示
- **模型广场**: 公开模型展示、收藏和交易功能

---

## 2. 技术栈

### 2.1 前端技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Vue | 3.4.x | 渐进式 JavaScript 框架 |
| UI库 | Element Plus | 2.6.x | Vue 3 组件库 |
| 构建工具 | Vite | 5.2.x | 下一代前端构建工具 |
| 状态管理 | Pinia | 2.1.x | Vue 官方状态管理库 |
| 路由 | Vue Router | 4.3.x | Vue 路由管理器 |
| HTTP | Axios | 1.6.x | HTTP 客户端 |
| 图表 | ECharts | 5.5.x | 数据可视化图表库 |
| 图标 | @element-plus/icons-vue | 2.3.x | Element Plus 图标库 |

### 2.2 后端技术栈

| 分类 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Spring Boot | 3.2.5 | Java 后端框架 |
| ORM | MyBatis Plus | 3.5.9 | MyBatis 增强工具 |
| 数据库 | MySQL | 8.x | 关系型数据库 |
| 安全 | Spring Security Crypto | 6.2.x | BCrypt 密码加密 |
| JWT | JJWT | 0.12.5 | JSON Web Token |
| Excel | EasyExcel | 3.3.3 | Excel 读写工具 |
| API文档 | SpringDoc OpenAPI | 2.3.0 | Swagger UI |

---

## 3. 项目架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端层 (Frontend)                        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────────┐ │
│  │ 登录页  │ │ 管理后台 │ │ 模型广场 │ │      公共组件         │ │
│  └────┬────┘ └────┬────┘ └────┬────┘ └──────────┬────────────┘ │
│       │            │            │                │              │
└───────┼────────────┼────────────┼────────────────┼──────────────┘
        │            │            │                │
        ▼            ▼            ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    接口层 (API Gateway)                         │
│              /api/auth /api/models /api/tools ...              │
└───────┬────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────┐
│                        后端层 (Backend)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────────┐ │
│  │Controller│→ │ Service  │→ │  Mapper  │→ │   Database      │ │
│  │  控制层  │  │  服务层  │  │  数据层  │  │   MySQL 8.x     │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                      中间件/配置                            │  │
│  │  JWT拦截器 | CORS配置 | MyBatis Plus配置 | 定时任务        │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 模块划分

| 模块 | 职责 | 说明 |
|------|------|------|
| **Auth** | 用户认证 | 登录、注册、Token管理 |
| **User** | 用户管理 | 个人信息、头像上传、密码修改 |
| **Model** | 模型管理 | 模型CRUD、数据隔离 |
| **Tool** | 工具管理 | 工具CRUD、数据隔离 |
| **Manufacturer** | 厂家管理 | 厂家CRUD |
| **Statistics** | 统计分析 | 数据统计、图表数据 |
| **ExcelImport** | Excel导入 | 批量导入模型和工具 |
| **Square** | 模型广场 | 公开模型、收藏、订单、消息 |

---

## 4. 目录结构

### 4.1 后端目录结构

```
backend/
├── src/main/java/com/model/management/
│   ├── common/                    # 通用类
│   │   ├── Result.java            # 统一响应封装
│   │   └── PageResult.java        # 分页响应封装
│   ├── config/                    # 配置类
│   │   ├── CorsConfig.java        # CORS配置
│   │   ├── JwtUtil.java           # JWT工具类
│   │   ├── JwtInterceptor.java    # JWT拦截器
│   │   ├── MybatisPlusConfig.java # MyBatis Plus配置
│   │   ├── WebConfig.java         # Web配置
│   │   └── PriceAlertTask.java    # 价格提醒定时任务
│   ├── controller/                # 控制器
│   │   ├── AuthController.java
│   │   ├── UserController.java
│   │   ├── ModelController.java
│   │   ├── ToolController.java
│   │   ├── ManufacturerController.java
│   │   ├── StatisticsController.java
│   │   ├── ExcelImportController.java
│   │   ├── FileUploadController.java
│   │   └── SquareController.java   # 模型广场控制器
│   ├── service/                   # 服务层
│   │   ├── AuthService.java
│   │   ├── UserService.java
│   │   ├── ModelService.java
│   │   ├── ToolService.java
│   │   ├── ...
│   │   └── impl/                  # 服务实现
│   │       ├── AuthServiceImpl.java
│   │       ├── UserServiceImpl.java
│   │       └── ...
│   ├── mapper/                    # Mapper接口
│   │   ├── UserMapper.java
│   │   ├── ModelMapper.java
│   │   ├── ToolMapper.java
│   │   ├── FavoriteMapper.java
│   │   ├── MessageMapper.java
│   │   ├── TransactionOrderMapper.java
│   │   └── ...
│   ├── entity/                    # 实体类
│   │   ├── User.java
│   │   ├── Model.java
│   │   ├── Tool.java
│   │   ├── Manufacturer.java
│   │   ├── Favorite.java
│   │   ├── Message.java
│   │   ├── TransactionOrder.java
│   │   └── PriceAlert.java
│   ├── dto/                       # 数据传输对象
│   │   ├── LoginDTO.java
│   │   ├── RegisterDTO.java
│   │   ├── ModelQueryDTO.java
│   │   ├── ModelSaveDTO.java
│   │   ├── ToolQueryDTO.java
│   │   ├── ToolSaveDTO.java
│   │   ├── FavoriteRequestDTO.java
│   │   ├── OrderCreateDTO.java
│   │   ├── OrderShipDTO.java
│   │   └── ...
│   ├── vo/                        # 视图对象
│   │   ├── LoginVO.java
│   │   ├── UserProfileVO.java
│   │   ├── ModelVO.java
│   │   ├── ToolVO.java
│   │   ├── StatisticsVO.java
│   │   ├── FavoriteVO.java
│   │   ├── MessageVO.java
│   │   └── ImportResultVO.java
│   ├── excel/                     # Excel导入相关
│   │   ├── ExcelModelRow.java
│   │   └── ExcelToolRow.java
│   └── ModelManagementApplication.java  # 启动类
├── src/main/resources/
│   ├── mapper/                   # MyBatis XML映射
│   │   ├── ModelMapper.xml
│   │   └── ToolMapper.xml
│   ├── db/migration/             # 数据库迁移脚本
│   │   ├── V2__add_model_square_tables.sql
│   │   └── V3__add_shipping_fields_to_order.sql
│   └── application.yml           # 应用配置
├── pom.xml                       # Maven配置
└── uploads/avatars/              # 头像上传目录
```

### 4.2 前端目录结构

```
frontend/
├── src/
│   ├── api/                      # API接口封装
│   │   ├── request.js            # Axios封装
│   │   ├── auth.js               # 认证API
│   │   ├── user.js               # 用户API
│   │   ├── model.js              # 模型API
│   │   ├── tool.js               # 工具API
│   │   ├── manufacturer.js       # 厂家API
│   │   ├── statistics.js         # 统计API
│   │   ├── excel.js              # Excel导入API
│   │   └── square.js             # 模型广场API
│   ├── router/                   # 路由配置
│   │   └── index.js
│   ├── stores/                   # Pinia状态管理
│   │   ├── useAuthStore.js       # 认证状态
│   │   └── useManufacturerStore.js # 厂家状态
│   ├── views/                    # 页面组件
│   │   ├── Login.vue             # 登录页
│   │   ├── Register.vue          # 注册页
│   │   ├── manager/              # 管理后台
│   │   │   ├── Layout.vue        # 主布局
│   │   │   ├── Dashboard.vue     # 数据统计
│   │   │   ├── ModelList.vue     # 模型管理
│   │   │   ├── ToolList.vue      # 工具管理
│   │   │   ├── ExcelImport.vue   # Excel导入
│   │   │   └── Profile.vue       # 个人信息
│   │   └── ModelSquare/          # 模型广场
│   │       ├── SquareLayout.vue  # 广场布局
│   │       ├── SquareHome.vue    # 广场首页
│   │       ├── SquareDetail.vue  # 详情页
│   │       ├── MyFavorites.vue   # 我的收藏
│   │       ├── Messages.vue      # 消息列表
│   │       ├── Chat.vue          # 聊天页面
│   │       └── MyOrders.vue      # 我的订单
│   ├── styles/                   # 全局样式
│   │   └── global.css
│   ├── App.vue                   # 根组件
│   └── main.js                   # 入口文件
├── public/                       # 静态资源
│   └── _redirects                # Vercel重定向配置
├── .env.production              # 生产环境变量
├── index.html                   # HTML模板
├── package.json                 # npm依赖
├── vite.config.js               # Vite配置
└── vercel.json                  # Vercel配置
```

---

## 5. 数据库设计

### 5.1 数据库表概览

| 表名 | 说明 | 状态 |
|------|------|------|
| `user` | 用户表 | 核心 |
| `manufacturer` | 厂家表 | 核心 |
| `model` | 模型表 | 核心 |
| `tool` | 工具表 | 核心 |
| `favorite` | 收藏表 | 广场功能 |
| `message` | 消息表 | 广场功能 |
| `transaction_order` | 交易订单表 | 广场功能 |
| `price_alert` | 价格提醒表 | 扩展功能 |

### 5.2 表结构详解

#### 5.2.1 user（用户表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| `username` | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| `password` | VARCHAR(255) | NOT NULL | 密码（BCrypt加密） |
| `email` | VARCHAR(100) | UNIQUE | 邮箱 |
| `phone` | VARCHAR(20) | | 手机号 |
| `nickname` | VARCHAR(50) | | 昵称 |
| `avatar` | VARCHAR(255) | | 头像URL |
| `status` | TINYINT | DEFAULT 1 | 状态：0-禁用，1-正常 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

#### 5.2.2 manufacturer（厂家表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 厂家ID |
| `name` | VARCHAR(100) | UNIQUE, NOT NULL | 厂家名称 |
| `description` | VARCHAR(500) | | 描述 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | TINYINT(1) | DEFAULT 0 | 逻辑删除 |

#### 5.2.3 model（模型表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 模型ID |
| `manufacturer_id` | BIGINT | FOREIGN KEY | 厂家ID |
| `name` | VARCHAR(200) | NOT NULL | 模型名称 |
| `price` | DECIMAL(10,2) | NOT NULL | 价格 |
| `remark` | VARCHAR(500) | | 备注 |
| `sold` | INT | DEFAULT 0 | 是否售出：0-未售，1-已售 |
| `user_id` | BIGINT | NOT NULL | 用户ID（数据归属） |
| `is_public` | INT | DEFAULT 0 | 是否公开：0-私有，1-公开 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | TINYINT(1) | DEFAULT 0 | 逻辑删除 |

#### 5.2.4 tool（工具表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 工具ID |
| `name` | VARCHAR(200) | NOT NULL | 工具名称 |
| `price` | DECIMAL(10,2) | NOT NULL | 价格 |
| `remark` | VARCHAR(500) | | 备注 |
| `sold` | INT | DEFAULT 0 | 是否售出：0-未售，1-已售 |
| `user_id` | BIGINT | NOT NULL | 用户ID（数据归属） |
| `is_public` | INT | DEFAULT 0 | 是否公开：0-私有，1-公开 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| `deleted` | TINYINT(1) | DEFAULT 0 | 逻辑删除 |

#### 5.2.5 favorite（收藏表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 收藏ID |
| `user_id` | BIGINT | NOT NULL | 用户ID |
| `item_type` | VARCHAR(20) | NOT NULL | 物品类型：MODEL/TOOL |
| `item_id` | BIGINT | NOT NULL | 物品ID |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 5.2.6 message（消息表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 消息ID |
| `from_user_id` | BIGINT | NOT NULL | 发送者ID |
| `to_user_id` | BIGINT | NOT NULL | 接收者ID |
| `content` | TEXT | NOT NULL | 消息内容 |
| `read` | TINYINT(1) | DEFAULT 0 | 是否已读 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

#### 5.2.7 transaction_order（交易订单表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 订单ID |
| `order_no` | VARCHAR(50) | UNIQUE, NOT NULL | 订单号 |
| `buyer_id` | BIGINT | NOT NULL | 买家ID |
| `seller_id` | BIGINT | NOT NULL | 卖家ID |
| `item_type` | VARCHAR(20) | NOT NULL | 物品类型：MODEL/TOOL |
| `item_id` | BIGINT | NOT NULL | 物品ID |
| `item_name` | VARCHAR(200) | NOT NULL | 物品名称（快照） |
| `price` | DECIMAL(10,2) | NOT NULL | 成交价格 |
| `status` | VARCHAR(20) | DEFAULT 'PENDING' | 订单状态 |
| `remark` | VARCHAR(500) | | 备注 |
| `tracking_number` | VARCHAR(100) | | 快递单号 |
| `express_company` | VARCHAR(100) | | 快递公司 |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

### 5.3 数据库关系图

```
user 1 ──* model (用户拥有多个模型)
user 1 ──* tool (用户拥有多个工具)
user 1 ──* favorite (用户有多个收藏)
user 1 ──* message (用户发送/接收消息)
user 1 ──* transaction_order (用户参与订单)
manufacturer 1 ──* model (厂家有多个模型)
```

---

## 6. API 接口文档

### 6.1 认证接口 `/api/auth`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/register` | POST | 用户注册 |

**登录请求体**:
```json
{
  "username": "string",
  "password": "string"
}
```

**登录响应体**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "token": "string",
    "user": {
      "id": 1,
      "username": "string",
      "nickname": "string",
      "email": "string",
      "avatar": "string"
    }
  }
}
```

### 6.2 用户接口 `/api/user`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/user/profile` | GET | 获取当前用户信息 |
| `/api/user/profile` | PUT | 更新用户信息 |
| `/api/user/password` | PUT | 修改密码 |

### 6.3 文件上传 `/api/upload`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/upload/avatar` | POST | 上传头像 |

### 6.4 模型管理 `/api/models`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/models` | GET | 分页查询模型列表 |
| `/api/models/{id}` | GET | 获取单个模型 |
| `/api/models` | POST | 新增模型 |
| `/api/models/{id}` | PUT | 更新模型 |
| `/api/models/{id}` | DELETE | 删除模型 |
| `/api/models/batch` | DELETE | 批量删除 |

### 6.5 工具管理 `/api/tools`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/tools` | GET | 分页查询工具列表 |
| `/api/tools/{id}` | GET | 获取单个工具 |
| `/api/tools` | POST | 新增工具 |
| `/api/tools/{id}` | PUT | 更新工具 |
| `/api/tools/{id}` | DELETE | 删除工具 |
| `/api/tools/batch` | DELETE | 批量删除 |

### 6.6 厂家管理 `/api/manufacturers`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/manufacturers` | GET | 获取厂家列表 |
| `/api/manufacturers` | POST | 新增厂家 |
| `/api/manufacturers/{id}` | PUT | 更新厂家 |
| `/api/manufacturers/{id}` | DELETE | 删除厂家 |

### 6.7 统计分析 `/api/statistics`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/statistics/overview` | GET | 获取统计概览 |
| `/api/statistics/manufacturer` | GET | 获取厂家统计 |
| `/api/statistics/price-distribution` | GET | 获取价格分布 |

### 6.8 Excel 导入 `/api/import`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/import/excel?type=model` | POST | 导入模型数据 |
| `/api/import/excel?type=tool` | POST | 导入工具数据 |
| `/api/import/template/model` | GET | 下载模型模板 |
| `/api/import/template/tool` | GET | 下载工具模板 |

### 6.9 模型广场 `/api/square`

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/square/models` | GET | 获取公开模型列表 |
| `/api/square/models/{id}` | GET | 获取公开模型详情 |
| `/api/square/favorites` | POST | 添加收藏 |
| `/api/square/favorites` | GET | 获取我的收藏 |
| `/api/square/favorites/{itemType}/{itemId}` | DELETE | 删除收藏 |
| `/api/square/orders` | POST | 创建订单 |
| `/api/square/orders` | GET | 获取我的订单 |
| `/api/square/orders/{id}` | GET | 获取订单详情 |
| `/api/square/orders/{id}/ship` | PUT | 发货 |
| `/api/square/orders/{id}/complete` | PUT | 确认收货 |
| `/api/square/messages` | GET | 获取消息列表 |
| `/api/square/messages` | POST | 发送消息 |
| `/api/square/messages/{userId}` | GET | 获取聊天记录 |
| `/api/square/messages/read` | PUT | 标记消息已读 |

---

## 7. 核心业务流程

### 7.1 用户认证流程

```
用户访问登录页 → 输入用户名密码 → 发送POST /api/auth/login
                    ↓
              后端验证用户名密码
                    ↓
              生成JWT Token
                    ↓
              返回Token和用户信息
                    ↓
              前端保存Token到localStorage
                    ↓
              路由守卫验证Token
                    ↓
              跳转到首页
```

### 7.2 数据隔离机制

```
用户请求 → JWT拦截器解析Token → 获取userId → 注入ThreadLocal
                    ↓
              Service层查询时自动添加user_id条件
                    ↓
              返回该用户的数据
```

### 7.3 模型广场流程

```
用户访问广场 → 获取公开模型列表(is_public=1) → 展示模型卡片
                    ↓
              用户点击模型 → 获取模型详情
                    ↓
              用户点击收藏 → 添加到收藏表
                    ↓
              用户点击购买 → 创建订单(PENDING状态)
                    ↓
              卖家发货 → 更新订单状态(SHIPPED)
                    ↓
              买家确认收货 → 更新订单状态(COMPLETED)
```

---

## 8. 配置说明

### 8.1 后端配置 (`application.yml`)

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/model_management?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true

# JWT配置
jwt:
  secret: mySecretKeyForJWTTokenGenerationAndValidation123456789
  expiration: 86400000  # 24小时(毫秒)

# 文件上传配置
file:
  upload:
    path: uploads/avatars
  access:
    url: http://localhost:8080/uploads
```

### 8.2 前端配置 (`vite.config.js`)

```javascript
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

---

## 9. 安全特性

| 特性 | 实现方式 | 说明 |
|------|----------|------|
| JWT认证 | JJWT库生成Token | 无状态认证 |
| 密码加密 | BCrypt算法 | 不可逆加密 |
| 数据隔离 | ThreadLocal + MyBatis拦截 | 用户只能访问自己的数据 |
| CORS配置 | Spring CORS Filter | 限制跨域来源 |
| SQL注入防护 | MyBatis Plus参数化查询 | 防止SQL注入 |
| 文件上传验证 | 类型和大小限制 | 防止恶意文件上传 |
| 逻辑删除 | MyBatis Plus全局配置 | 防止物理删除 |

---

## 10. 开发规范

### 10.1 代码风格

- **前端**: 遵循 Vue 3 Composition API 最佳实践
- **后端**: 遵循 RESTful API 设计规范
- **命名**: 统一使用驼峰命名法
- **注释**: 关键业务逻辑添加中文注释

### 10.2 Git 提交规范

| 前缀 | 说明 |
|------|------|
| `feat` | 新功能 |
| `fix` | 修复 bug |
| `docs` | 文档更新 |
| `style` | 代码格式调整 |
| `refactor` | 重构代码 |
| `test` | 测试相关 |
| `chore` | 构建/工具链相关 |

---

## 11. 快速启动

### 11.1 前置要求

- Node.js >= 16.x
- JDK 17+
- MySQL 8.x
- Maven 3.6+

### 11.2 步骤

**1. 创建数据库**
```sql
CREATE DATABASE IF NOT EXISTS model_management 
DEFAULT CHARACTER SET utf8mb4 
DEFAULT COLLATE utf8mb4_unicode_ci;
```

**2. 启动后端**
```bash
cd backend
mvn spring-boot:run
```
访问 Swagger UI: http://localhost:8080/swagger-ui.html

**3. 启动前端**
```bash
cd frontend
npm install
npm run dev
```
访问: http://localhost:5173

---

## 12. 部署指南

### 12.1 前端构建

```bash
cd frontend
npm run build
# 生成的 dist 目录部署到 Nginx 或 Vercel
```

### 12.2 后端打包

```bash
cd backend
mvn clean package
java -jar target/model-management-1.0.0.jar
```

### 12.3 Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /uploads {
        alias /path/to/backend/uploads;
        expires 30d;
    }
}
```

---

## 13. 后续优化计划

### 13.1 功能增强

- [ ] 权限管理（RBAC）
- [ ] 操作日志记录
- [ ] 数据导出功能
- [ ] 更多统计维度
- [ ] 消息通知系统（WebSocket）

### 13.2 性能优化

- [ ] Redis 缓存热点数据
- [ ] 前端资源 CDN 加速
- [ ] 后端接口响应压缩
- [ ] 数据库读写分离
- [ ] 图片压缩和缩略图

### 13.3 用户体验

- [ ] 国际化支持
- [ ] 主题切换
- [ ] 快捷键支持
- [ ] 头像裁剪功能
- [ ] 上传进度显示

---

**最后更新时间**: 2026-05-28